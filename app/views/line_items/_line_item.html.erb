<li>
	<% if line_item == @current_item %>
	<script>
	  yip();
	</script>
	<% end %>
	<%= line_item.quantity %> &times; <%= line_item.book.title %> - 
	<%= number_to_currency(line_item.total_price) %>
</li>